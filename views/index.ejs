<html>
    <%- include('partials/header.ejs'); -%>
    <body>
      <div id="top-block">
      <% var user = 0 %>
        <% if (user) { %>
            <h1>Welcome, @<%= user.name %></h1>
          <% } else { %>
            <h1>Welcome, @guest<%= Math.floor(Math.random() * 999999999) %></h1>
            <% } %>
          </div>

    </body>
</html>